@model IEnumerable<PBL3.Models.Lop>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
q
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LOP_START)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LOP_END)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LOP_START)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LOP_END)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {id = item.LOP_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LOP_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LOP_ID })
        </td>
    </tr>
}

</table>
